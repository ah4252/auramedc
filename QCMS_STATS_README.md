# قسم إدارة إحصائيات QCMs

تم إضافة قسم جديد في لوحة التحكم يتيح للمسؤول التحكم الكامل بالإحصائيات الثلاث المعروضة في قسم الاختبارات.

## الميزات الجديدة ✨

### 1. الإحصائيات الثلاث المدارة
- **دقة المناهج** (QCMS Accuracy): نسبة دقة وشمولية المناهج الدراسية
- **نماذج الاختبارات** (Exam Count): عدد نماذج الاختبارات المتاحة
- **المواد والتخصصات** (Subject Count): عدد المواد والتخصصات المغطاة

### 2. الوصول إلى صفحة الإحصائيات
- **المسار**: `/admin/qcms/stats`
- **الزر السريع**: زر "الإحصائيات" في رأس صفحة إدارة QCMs

### 3. واجهة الإدارة
الصفحة توفر:
- حقول إدخال منفصلة لكل إحصائية
- معاينة مباشرة للإحصائيات كما ستظهر للمستخدمين
- زر حفظ سهل الاستخدام
- رسائل تأكيد النجاح أو الخطأ

## التغييرات التقنية

### 1. نموذج البيانات (Prisma Schema)
تمت إضافة ثلاثة حقول جديدة إلى جدول `SiteSettings`:
```prisma
qcmsAccuracy       String?  @default("100%")
qcmsExamCount      String?  @default("500+")
qcmsSubjectCount   String?  @default("20+")
```

### 2. إجراءات الخادم (Server Actions)
تم تحديث `src/app/actions/settings.ts`:
- دعم قراءة وكتابة الحقول الجديدة
- التحقق من صحة البيانات

### 3. API Endpoints
جديد: `src/app/api/admin/settings/route.ts`
- **GET**: جلب إعدادات الموقع الحالية
- **PUT**: تحديث الإعدادات الجديدة

### 4. مكون العميل (Client Component)
جديد: `src/app/admin/qcms/stats/QcmsStatsClient.tsx`
- واجهة تفاعلية لإدارة الإحصائيات
- معاينة مباشرة
- معالجة الأخطاء والنجاحات

## خطوات التثبيت

### 1. تحديث قاعدة البيانات
```bash
# في مجلد المشروع
cd c:\Users\ahmed\Downloads\my-main\my-main

# إنشاء migration
npx prisma migrate dev --name add_qcms_stats_fields
```

إذا واجهت مشكلة في PowerShell، استخدم:
```powershell
powershell.exe -ExecutionPolicy Bypass -Command "npx prisma migrate dev --name add_qcms_stats_fields"
```

أو استخدم الأمر البديل:
```bash
npx prisma db push
```

### 2. إعادة بناء المشروع
```bash
npm run dev
```

## طريقة الاستخدام

### كمسؤول:
1. توجه إلى `/admin/qcms`
2. اضغط على زر "الإحصائيات" في الأعلى
3. عدّل القيم الثلاث المطلوبة
4. اضغط "حفظ الإحصائيات"
5. الإحصائيات ستُحدَّث فوراً على الموقع

### أمثلة على القيم:
- **دقة المناهج**: `100%`, `98%`, `95%`
- **نماذج الاختبارات**: `500+`, `1000+`, `2500+`
- **المواد والتخصصات**: `20+`, `50+`, `100+`

## حيث تظهر الإحصائيات على الموقع

### 1. صفحة QCMs الرئيسية (`/qcms`)
يتم عرض الإحصائيات في بطاقات metrics في القسم الأول

### 2. قسم الاختبارات (`/courses?tab=qcms`)
قد تُعرض الإحصائيات في رأس القسم أو في بطاقات عرض خاصة

## ملفات مُعدَّلة

- `prisma/schema.prisma` - إضافة الحقول الثلاث
- `src/app/actions/settings.ts` - دعم الحقول الجديدة
- `src/app/admin/qcms/QcmsAdminClient.tsx` - إضافة رابط الإحصائيات

## ملفات جديدة

- `src/app/admin/qcms/stats/page.tsx` - صفحة الإحصائيات
- `src/app/admin/qcms/stats/QcmsStatsClient.tsx` - مكون إدارة الإحصائيات
- `src/app/api/admin/settings/route.ts` - API endpoints للإعدادات

## استكشاف الأخطاء

### إذا لم تظهر الصفحة:
- تأكد من تشغيل الخادم: `npm run dev`
- تحقق من توفر قاعدة البيانات
- أعد تحميل الصفحة في المتصفح

### إذا لم يتم حفظ الإحصائيات:
- تحقق من توفر قاعدة البيانات
- تأكد من نفاذك كمسؤول
- تحقق من console في DevTools من أجل رسائل الخطأ

## التطويرات المستقبلية

يمكن تطويرها لاحقاً بـ:
- إحصائيات إضافية
- رسوم بيانية توضيحية
- تقارير شهرية
- تتبع التغييرات التاريخية
